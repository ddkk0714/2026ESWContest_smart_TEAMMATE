# DESKMATE dev broker config for the Pi 4 (ATLAS). Plain MQTT, LAN/hotspot only.
listener 1883 0.0.0.0
allow_anonymous true
# ATLAS has no "mosquitto" user; the SSH shell runs as root.
user root
persistence false
log_dest stdout
